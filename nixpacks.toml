[phases.setup]
nixPkgs = ["nodejs_24", "python311", "python311Packages.pip"]

[phases.install]
cmds = ["npm ci", "python3 -m pip install --break-system-packages -r requirements.txt"]

[start]
cmd = "npm start"
